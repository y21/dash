[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(dash_lints)'] }

[dependencies]
dash_vm = { path = "../../../crates/dash_vm" }
